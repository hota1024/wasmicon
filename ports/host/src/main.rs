//! PC 上で `.wasm` を実行し、mock HAL のトレース（abi-spec §9 形式）を出すポート。
//!
//! HANDOFF §5 Phase 2 の完了条件はここで検証する。

fn main() {
    eprintln!("wasmicon-host: 未実装（HANDOFF §5 Phase 2）");
    std::process::exit(1);
}
