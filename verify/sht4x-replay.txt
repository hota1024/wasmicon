# 記録済み SHT40（SHT4x）応答（docs/handoff.md §5 Phase 6）。
# 1 行 = 1 回の i2c 読み出しに返すバイト列（16 進、空白区切り）。
# `#` で始まる行と空行は無視する。
#
# この応答は T=23.44C / RH=51.08% になる合成データ。
# バイト列は SHT31 時代のものをそのまま使っている（SHT4x とフレームも CRC-8 も
# 同じなので有効）。湿度の式が変わったので表示値だけ 45.66% から動いた。
# 実機から記録したものに差し替えるのは Phase 6（SHT40 実機が要る）。
# 内訳: T MSB, T LSB, CRC8, RH MSB, RH LSB, CRC8
64 21 e0 74 e9 70
