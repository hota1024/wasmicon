# 記録済み SHT31 応答（HANDOFF §5 Phase 6）。
# 1 行 = 1 回の i2c 読み出しに返すバイト列（16 進、空白区切り）。
# `#` で始まる行と空行は無視する。
#
# この応答は T=23.44C / RH=45.66% になるよう作った合成データ。
# 実機から記録したものに差し替えるのは Phase 6。
# 内訳: T MSB, T LSB, CRC8, RH MSB, RH LSB, CRC8
64 21 e0 74 e9 70
