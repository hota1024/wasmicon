[package]
name = "wasmicon-hal"
description = "ゲスト向け HAL バインディング。生成された extern 宣言 + 安全ラッパ"
# guest workspace は apps/。root workspace から exclude されているので明示する。
workspace = "../../apps"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
