[package]
name = "wasmicon-core"
description = "Wasmicon の Core Wasm インタプリタ。no_std、依存クレートゼロ、alloc 不使用"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

# 依存クレートを追加しない（docs/handoff.md §2-11）。コア本体は依存ゼロを保つ。
[dependencies]

# spec テストランナー（runtime/tests/）だけは依存を持ってよい。
[dev-dependencies]
serde_json = "1"
